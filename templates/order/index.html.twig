{% extends 'base.html.twig' %}

{% block title %}Valider ma commande - French boutique{% endblock %}

{% block content %}
<h2> Je passe ma commande</h2>
<p> choissisez vos préférences avant de passez votre commande sur French boutique.</p>
<hr>
<div class="row">
<div class ="col-md-6">


{% set formHtml %}
    {{ form_start(form, {action:path('app_order_recap')}) }}

    {{ form_end(form) }}
{% endset %}

{{ formHtml|replace({'[br]' :'<br/>'})|raw}}

</div>

<div class ="col-md-6">
<div class ="text-center">
<b>Récapitulatif de ma commande</b><br>
</div>

<div class="order-summary">
{% for key,produit in cart %}
<div class ="row {% if key > 0 %}mt-2{% endif %}">
<div class =" col-2">
<img src ="/img/{{ produit.produit.illustration}}" alt ="{{produit.produit.name}}"height="75px">
</div>

<div class =" col-8 my-auto">
{{ produit.produit.name }}<br>
 <small>{{ produit.produit.subtitle }}</small>
</div>

<div class =" col-2 my-auto"> 
x {{produit.quantity}} 
</div>
</div>


{% endfor %}
</div>


</div>

</div>
{% endblock %}
